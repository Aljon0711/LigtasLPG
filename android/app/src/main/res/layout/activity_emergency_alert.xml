<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#D32F2F"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="28dp">

    <TextView
        android:id="@+id/emergencyTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="LigtasLPG Emergency"
        android:textAllCaps="true"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/emergencyBody"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="Gas leak detected. Open the app immediately."
        android:textColor="#FFFFFF"
        android:textSize="16sp" />

    <Button
        android:id="@+id/emergencyOpenButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="36dp"
        android:backgroundTint="#FFFFFF"
        android:padding="14dp"
        android:text="Open Alert"
        android:textAllCaps="true"
        android:textColor="#D32F2F"
        android:textStyle="bold" />

    <Button
        android:id="@+id/emergencyMuteButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:backgroundTint="#B71C1C"
        android:padding="12dp"
        android:text="Mute sound"
        android:textAllCaps="false"
        android:textColor="#FFFFFF" />
</LinearLayout>
